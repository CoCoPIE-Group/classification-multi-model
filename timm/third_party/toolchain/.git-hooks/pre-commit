#!/usr/bin/bash
if [ -z "${VIRTUAL_ENV}" ]; then
    echo "Please enter the virtual environment before commit."
    exit 1
fi

# pylint check tools
pylint tools

if [ "$?" != "0" ]; then
    echo "Please correct lint errors above before commit."
    exit 1
fi

